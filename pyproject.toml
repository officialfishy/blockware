[project]
name = "blockware"
version = "0.2.0"
description = "Blockware CLI wallet tool"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "typer>=0.12.0",
  "rich>=13.7.0",
  "cryptography>=42.0.0",
  "bip-utils>=2.9.0",
]
[project.scripts]
blockware = "blockware.cli:main"

